Evaluation-only test-domain control.

Use test.txt as the strict file split and object_ignore_override.json as the evaluation GT neutral-region override. Extra eligible source Sedan boxes are ignored, not counted as positives or negatives.
